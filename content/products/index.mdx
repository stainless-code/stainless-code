---
title: Products
description: When to use Codemap, Layers, or Persist — open-source TypeScript and JavaScript libraries from Stainless Code, with docs and npm links.
---

Need a structural index for agents, awaitable overlays, or hydration-aware store persistence? Start from the catalog below. Why we ship this way: [About](/about).

## Libraries

| Product | One-liner | Docs | npm |
| ------- | --------- | ---- | --- |
| **Codemap** | Local codebase intelligence for AI agents — SQL and recipes instead of scanning the tree | [docs](https://stainless-code.com/codemap) | [`@stainless-code/codemap`](https://npmx.dev/@stainless-code/codemap) |
| **Layers** | Modals are just async functions you forgot to `await` | [docs](https://stainless-code.com/layers) | [`@stainless-code/layers`](https://npmx.dev/@stainless-code/layers) |
| **Persist** | Any store, any storage, one middleware — no flash | [docs](https://stainless-code.com/persist) | [`@stainless-code/persist`](https://npmx.dev/@stainless-code/persist) |

## When to reach for each

- **Codemap** — Use when an agent (or tool) needs a local structural map of a codebase — symbols, imports, and calls — as SQL and recipes instead of scanning the tree.
- **Layers** — Use when overlays (modals, drawers, toasts) should be awaitable calls with a clear stack contract, not ad-hoc dialog state.
- **Persist** — Use when a reactive store needs hydration-aware persistence across storage backends — one middleware, no flash of empty state.

Different problems. No forced stack — pick one package or several.

## Legacy / adjacent

Older React utilities under the same org (npm-homed; no dedicated docs site today):

- [`@stainless-code/react-custom-events`](https://npmx.dev/@stainless-code/react-custom-events)
- [`@stainless-code/react-paginate`](https://npmx.dev/@stainless-code/react-paginate)
- [`@stainless-code/react-memo`](https://npmx.dev/@stainless-code/react-memo)

Archive / npm links — not the primary product index.
